<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://dcwire.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://dcwire.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-05-11T13:55:57+00:00</updated><id>https://dcwire.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">Writing GPT2 from scratch in pytorch</title><link href="https://dcwire.github.io/blog/2026/writing-your-first-cuda-kernel/" rel="alternate" type="text/html" title="Writing GPT2 from scratch in pytorch"/><published>2026-05-01T00:00:00+00:00</published><updated>2026-05-01T00:00:00+00:00</updated><id>https://dcwire.github.io/blog/2026/writing-your-first-cuda-kernel</id><content type="html" xml:base="https://dcwire.github.io/blog/2026/writing-your-first-cuda-kernel/"><![CDATA[<blockquote> <p><strong>Draft</strong> — replace this with your own content. This is a starter template.</p> </blockquote> <p>Every CUDA tutorial shows you the same vector addition kernel. It compiles, it runs, and then you’re left wondering: why is it slower than just running it on the CPU?</p> <p>The answer is almost always the same: you wrote a <strong>memory-bound kernel with no reuse</strong>, and you launched it with a thread hierarchy that has no idea how the hardware actually works.</p> <p>This post is the mental model I wish I’d had on day one.</p> <hr/> <h2 id="the-hardware-youre-actually-talking-to">The Hardware You’re Actually Talking To</h2> <p>Before writing a single line of CUDA, it helps to know what you’re targeting. An NVIDIA GPU is organized roughly like this:</p> <ul> <li><strong>SMs (Streaming Multiprocessors)</strong> — the physical compute units. A modern GPU has tens to hundreds of them.</li> <li><strong>Warps</strong> — groups of 32 threads that execute in lockstep on an SM. This is the real unit of execution, not the thread.</li> <li><strong>Shared memory</strong> — fast, on-chip SRAM local to an SM (~48–100KB depending on config). Orders of magnitude faster than global memory.</li> <li><strong>Global memory</strong> — the big DRAM (HBM on datacenter GPUs). High bandwidth, but high latency.</li> </ul> <p>Your job as a kernel writer is to keep the SMs fed with work and minimize unnecessary global memory traffic.</p> <hr/> <h2 id="the-memory-hierarchy-is-everything">The Memory Hierarchy Is Everything</h2> <p>A naive kernel looks like this:</p> <div class="language-cuda highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">__global__</span> <span class="kt">void</span> <span class="nf">add</span><span class="p">(</span><span class="kt">float</span><span class="o">*</span> <span class="n">a</span><span class="p">,</span> <span class="kt">float</span><span class="o">*</span> <span class="n">b</span><span class="p">,</span> <span class="kt">float</span><span class="o">*</span> <span class="n">c</span><span class="p">,</span> <span class="kt">int</span> <span class="n">n</span><span class="p">)</span> <span class="p">{</span>
    <span class="kt">int</span> <span class="n">i</span> <span class="o">=</span> <span class="n">blockIdx</span><span class="p">.</span><span class="n">x</span> <span class="o">*</span> <span class="n">blockDim</span><span class="p">.</span><span class="n">x</span> <span class="o">+</span> <span class="n">threadIdx</span><span class="p">.</span><span class="n">x</span><span class="p">;</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">i</span> <span class="o">&lt;</span> <span class="n">n</span><span class="p">)</span> <span class="n">c</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">=</span> <span class="n">a</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">+</span> <span class="n">b</span><span class="p">[</span><span class="n">i</span><span class="p">];</span>
<span class="p">}</span>
</code></pre></div></div> <p>Every thread reads two values from global memory and writes one. There is zero reuse. The GPU’s arithmetic units spend most of their time waiting for memory. This is what “memory-bound” means on the roofline model.</p> <p>The fix is <strong>data reuse</strong> — either through shared memory (explicit) or by restructuring access patterns so the L2 cache helps you (implicit).</p> <hr/> <h2 id="thread-hierarchy-blocks-and-warps">Thread Hierarchy: Blocks and Warps</h2> <p>CUDA threads are organized into blocks, and blocks into a grid. But what actually matters for performance is the warp:</p> <ul> <li>32 threads = 1 warp</li> <li>All threads in a warp execute the same instruction at the same time (SIMT)</li> <li>If threads in a warp take different branches, both paths execute and inactive threads are masked — this is <strong>warp divergence</strong></li> </ul> <p>When you write <code class="language-plaintext highlighter-rouge">blockDim.x = 256</code>, you’re really saying “launch 8 warps per block.” The SM schedules warps, not threads. If one warp is stalled waiting on memory, the SM switches to another — this is <strong>latency hiding</strong>, and it only works if you have enough warps in flight.</p> <hr/> <h2 id="what-to-measure-before-you-optimize">What to Measure Before You Optimize</h2> <p>Don’t guess. Run Nsight Compute on your kernel and look at:</p> <ul> <li><strong>Memory throughput</strong> vs. theoretical peak — are you hitting the roof?</li> <li><strong>Compute throughput</strong> — are your CUDA cores busy?</li> <li><strong>Stall reasons</strong> — <code class="language-plaintext highlighter-rouge">stall_long_sb</code> means memory latency isn’t being hidden; <code class="language-plaintext highlighter-rouge">stall_mio_throttle</code> means you’re hammering the memory system</li> </ul> <p>Once you know which roof you’re hitting, you know what to fix.</p> <hr/> <h2 id="next-steps">Next Steps</h2> <p><em>Fill in with what you build from here — shared memory tiling, occupancy experiments, etc.</em></p>]]></content><author><name></name></author><category term="pytorch"/><category term="pytorch"/><category term="llm"/><category term="attention"/><summary type="html"><![CDATA[Deconstructing the GPT-2 architecture to justify the transition from standard frameworks to manual CUDA kernel optimization and automated compiler fusion]]></summary></entry></feed>